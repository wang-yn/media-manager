services:
  media-manager:
    build: .
    container_name: media-manager
    environment:
      - TZ=Asia/Shanghai
      - TMDB_API_KEY=${TMDB_API_KEY:-}
    volumes:
      - ./config:/config
      - ./media:/media
    ports:
      - "8000:8000"
    restart: unless-stopped
